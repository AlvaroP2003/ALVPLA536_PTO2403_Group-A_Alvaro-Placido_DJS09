// Enums that give access to its properties

export enum Permissions {               // Exported to './index.ts'
    ADMIN = 'ADMIN', 
    READ_ONLY = 'READ_ONLY'
}

export enum LoyaltyUser {               //Exported to './index/ts' and '/interfaces.ts'
    GOLD_USER = 'GOLD_USER',
    SILVER_USER = 'SILVER_USER',
    BRONZE_USER = 'BRONZE_USER'
}